-r requirements.txt
mediapipe>=0.10        # pose estimation from a webcam (auto-downloads weights, CPU)
opencv-python>=4.8     # camera capture + the live window
sounddevice>=0.4       # built-in real-time synth (no DAW needed); bundles PortAudio
python-rtmidi>=1.5     # optional: MIDI out to a DAW instead of the built-in synth
python-osc>=1.8        # optional: OSC out
